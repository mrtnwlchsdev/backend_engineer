/*
    Los operadores de comparacion permiten comparar 2 valores, dando como resultado de la comparacion un valor booleano

    < Menor que
    > Mayor que
    <= Menor o igual que
    >= Mayor o igual que
    === Exactamente igual que
    !== Exactamente distinto que
*/

if (10 > 4) {
    console.log("10 is greater than 4")
}